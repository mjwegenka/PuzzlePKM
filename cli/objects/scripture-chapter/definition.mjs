// DEC-77: chapters are a first-class object type — the unit scripture is
// browsed by — distinct from the verse-level `scripture` references that roll
// up into them.
export const scriptureChapterDefinition = {
  type: 'scripture-chapter',
  aliases: ['scripture-chapter', 'scripture-chapters', 'chapter', 'chapters'],
};
